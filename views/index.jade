extends layout

block content
  div#landing-videos
    each video, i in videos
      section
        a(href="/video/#{video._id}")     
          video(width="200px")
            source(src="#{video.url}")
        div.info-overlay      
          span #{video.title}
